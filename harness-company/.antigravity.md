You are working on harness-company, a Next.js 15 company introduction / portfolio website template with an admin panel.

## Stack
- Next.js 15 (App Router), React 19, TypeScript (strict)
- Tailwind CSS v4 (CSS-first `@theme`, no config file), Framer Motion, Lucide React
- Neon (serverless Postgres) — contact storage, server-side only, optional
- Password-based admin auth (server `ADMIN_PASSWORD` + httpOnly cookie)
- Noto Sans KR (placeholder font, swappable in `layout.tsx`)

## Architecture
- All site content is centralized in `src/lib/data.ts`
- Colors/theme defined in the `@theme` block of `src/app/globals.css` (no `tailwind.config.ts`)
- Neon handles contact form storage; all DB access is server-side only (`src/lib/db.ts`)
- Admin auth is a single server-side password; login sets an httpOnly session cookie
- Admin panel at `/admin` is protected by `ProtectedRoute` component
- `useAuth` hook in `src/hooks/useAuth.ts` reads session state from `/api/admin/session`

## Key Files
- `src/lib/data.ts` — Single source of truth for ALL site text and configuration
- `src/app/globals.css` — `@theme` tokens (neutral placeholder palette, `--font-sans`)
- `src/app/layout.tsx` — Root layout with Noto Sans KR font and global metadata
- `src/components/ui/AnimatedSection.tsx` — Intersection observer + Framer Motion wrapper
- `src/lib/db.ts` — Neon client + contact queries (server-only, null-safe)
- `src/lib/auth.ts` — Admin session token + cookie check (server-only)
- `src/lib/api.ts` — Typed client fetch helpers for the `/api/*` routes
- `src/app/api/` — `contact`, `admin/login`, `admin/logout`, `admin/session`, `admin/contacts`
- `src/hooks/useAuth.ts` — Cookie-session auth state hook

## Component Organization
- `src/components/ui/` — Reusable primitives (Button, Card, AnimatedSection, PageHeader)
- `src/components/sections/` — Home page sections (Hero, About, Services, Portfolio, Testimonials, Contact)
- `src/components/layout/` — Navbar (with mobile menu), Footer
- `src/components/admin/` — Admin dashboard (AdminLayout, ContactTable, ProtectedRoute)

## Customization Guide
| What to change | Where to edit |
|---|---|
| All text/content | `src/lib/data.ts` |
| Colors / theme | `src/app/globals.css` `@theme` block |
| Font | `src/app/layout.tsx` (`next/font/google`) |
| Contact form → DB | `src/lib/db.ts` + `src/app/api/contact/route.ts` |
| Admin auth | `src/lib/auth.ts` + `src/app/api/admin/*` |
| Admin table columns | `src/components/admin/ContactTable.tsx` |
| Animation settings | `src/components/ui/AnimatedSection.tsx` |
| DB / admin credentials | `.env.local` (`DATABASE_URL`, `ADMIN_PASSWORD`) |

## Conventions
- Named exports for all components
- `'use client'` directive on interactive components
- `cn()` utility for conditional Tailwind classes
- Lucide React icons imported individually by name
- Comments mark customization points throughout the codebase

## Deployment: GitHub → Vercel
1. GitHub push → Vercel 자동 재배포 (main 브랜치)
2. Vercel 환경변수: `.env.example` 키 전부 입력
3. 환경변수 수정: Vercel 대시보드 → Settings → Environment Variables

## DB / 인증: Neon + 비밀번호
이 템플릿은 Neon(서버리스 PostgreSQL) 사용. DB 접근은 서버 사이드 전용(`src/lib/db.ts`).
Vercel → Storage → Neon 연동 시 `DATABASE_URL` 자동 주입, 로컬은 `vercel env pull .env.local`.
`DATABASE_URL` 미설정이어도 실행 가능(연락처 폼은 "저장 설정 전", 관리자 표는 빈 상태).
관리자 인증은 서버 `ADMIN_PASSWORD` + httpOnly 쿠키(별도 사용자 계정 없음).

## SEO 설정
- `src/app/sitemap.ts` — sitemap.xml 자동 생성
- `src/app/robots.ts` — robots.txt 자동 생성 (admin 경로 차단 포함)
- `.env.local`에 `GOOGLE_SITE_VERIFICATION`, `NAVER_SITE_VERIFICATION` 입력
- `public/og-image.png` (1200×630px) 추가 시 SNS 공유 미리보기 활성화

## 도메인 연결 (가비아 → Cloudflare → Vercel)
1. 가비아에서 도메인 구매
2. Cloudflare에 도메인 등록 → 네임서버 복사
3. 가비아 네임서버를 Cloudflare 네임서버로 변경
4. Vercel Settings → Domains에 도메인 추가
5. Cloudflare DNS에 CNAME 레코드 추가 (Proxy: DNS only)
6. `NEXT_PUBLIC_SITE_URL` 실제 도메인으로 변경 후 재배포
